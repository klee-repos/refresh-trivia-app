const initialAppState = {
    appList: true,
    blackjack: false,
}

export default function apps (state = initialAppState, action) {
    switch(action.type) {
        default:
            return state
    }
}